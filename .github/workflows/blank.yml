name: Lint Files

on: [pull_request]

jobs:
  job1:
    runs-on: ubuntu-latest
    steps:
      - uses: "actions/checkout@v2"
      - uses: "actions/setup-python@v2"
        with:
            python-version: "3.8"
      - name: Install SQLFluff
        run: "pip install sqlfluff==1.4.2"
      
      - name: step1
        id: vars1
        run: |
          echo $(sqlfluff lint --format human)
          declare OUTP=`sqlfluff lint --format human`
          echo test1=$OUTP >> $GITHUB_OUTPUT
          declare OUTP2=`sqlfluff lint --format human`
          OUTP3=$OUTP2 | cut -d "]" -f 2
          echo test2=$OUTP3 >> $GITHUB_OUTPUT
          echo $OUTP2
          
      - name: step2
        if: success() && steps.step1.outcome != 'failure'
        uses: mshick/add-pr-comment@v2
        with:
          message: ${{steps.vars1.outputs.test1}}
          allow-repeats: true
          
      - name: step3
        if: success() && steps.step1.outcome != 'failure'
        uses: mshick/add-pr-comment@v2
        with:
          message: ${{steps.vars1.outputs.test2}}
          allow-repeats: true
          
      - name: step4
        if: success() && steps.step1.outcome != 'failure'
        run: |
          declare vars=${{needs.job1.outputs.test}}
          echo $vars

  job2:
    runs-on: ubuntu-latest
    needs: job1
    steps:
      - name: action-lint-failure-file
        if: failure() && steps.step1.outcome != 'success'
        run: echo ${{needs.job1.outputs.output1}}
 
      - name: action-lint-success
        if: success() && steps.step1.outcome != 'failure'
        run: ${{needs.job1.outputs.output1}}
